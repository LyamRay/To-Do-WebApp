# SQLite database path
todo.database.path=todo.db

# Server port (adjust if needed)
server.port=8080

# Enable CORS if needed for React frontend on localhost:3000
spring.mvc.cors.allowed-origins=http://localhost:3000
